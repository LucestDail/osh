# OSH — 상세 구현 계획

> **표기 범례** (2026-09-16 전수 대조)
> `[x]` 했다 · `[~]` 다르게 해결했다 · `[-]` 안 하기로 했다 · `[ ]` 아직
>
> ⚠️ `[~]`·`[-]` 는 표준 체크박스 문법이 아니라 렌더러에 따라 `[ ]` 처럼 보인다.
> 그래서 **항목마다 `←` 뒤에 라벨**을 붙였다.
>
> 🔴 이 PLAN 은 2026-04-15 작성 후 **한 번도 체크된 적이 없었다**(`[x]` 0건).
> 41건이 "미완" 이었으나 전수 대조 결과 **22건은 이미 완료**, 2건은 안 하기로 한 것이었다.
> **진짜 잔여는 17건**이고 대부분 **M3 UI 리뉴얼**(Phase 2)과 **뷰 프리셋**이다.
> 🔴 특히 Phase 1(안정화·보안) 9건 중 **8건이 이미 끝나 있었다** — 보안 항목이 미완으로
> 보이면 "아직 취약하다" 로 읽힌다. 실제로는 `18ee419`(2026-08-27)에서 해결됐다.


## 1. 프로젝트 비전

개인용 실시간 정보 대시보드. 날씨, 교통, 재난, 뉴스, Gemini 요약 등 특화된 기능을 더 편리하고 확장 가능하게 개선.

## 2. 현재 상태

- Spring Boot 3.4.0-SNAPSHOT (불안정)
- 날씨(OpenWeatherMap), 교통, 재난, 뉴스, 시스템 정보
- SSE 기반 실시간 스트리밍 (1초/60초 간격)
- 스케줄러로 60초마다 데이터 갱신
- Thymeleaf 웹 UI
- Gemini AI 대시보드 요약
- 이중 DB (메인 + 뉴스 MySQL)
- WeatherInterface에 API 키 하드코딩
- Actuator 전체 노출

## 3. 디자인 시스템

**Google Material Design 3 (M3) 준용**

### M3 적용 방침
현재 Thymeleaf 기반이므로, 정적 HTML/CSS에 M3 원칙을 점진적으로 적용:

### 컬러 시스템
| 토큰 | Light | Dark | 용도 |
|------|-------|------|------|
| Primary | `#1B6B52` | `#7EDCB5` | 주요 액션, 강조 |
| On Primary | `#FFFFFF` | `#003828` | Primary 위 텍스트 |
| Secondary | `#4A635B` | `#B2CCBF` | 보조 요소 |
| Tertiary | `#3F6374` | `#A3CDDF` | 날씨/정보 강조 |
| Surface | `#F5FBF6` | `#1A1C1A` | 카드/배경 |
| Error | `#BA1A1A` | `#FFB4AB` | 재난/경고 |

> 대시보드 특성 반영: Primary를 차분한 그린 계열로 설정 (데이터 시각화에 적합)

### 타이포그래피
- M3 Type Scale: `headlineSmall`, `titleMedium`, `bodyLarge`, `labelMedium`
- 한글: Pretendard 또는 Noto Sans KR
- 숫자/데이터: Roboto Mono (고정폭)

### 컴포넌트
- M3 카드: Filled / Outlined / Elevated 혼용
- M3 Navigation: Top App Bar + Navigation Rail (사이드)
- 다크/라이트 모드 전환 지원
- `border-radius: 12px` (M3 Medium)

---

## 4. 단계별 구현 계획

### Phase 1 — 안정화 + 보안 수정 (2주)

**1.1 Spring Boot 버전 수정**
- [x] `3.4.0-SNAPSHOT` → `3.4.x` 정식 릴리즈로 변경  ← **됨**: Spring Boot **3.5.11 정식**(3.4.0-SNAPSHOT 아님)
- [x] 스냅샷 저장소 참조 제거  ← **됨**: `<repositories>` 스냅샷 선언 없음 (`0.0.1-SNAPSHOT` 은 이 앱 자신의 버전이라 정상)
- [-] 의존성 호환성 확인  ← **안 함**: 판정 기준이 없는 항목이다. "호환된다" 를 무엇으로 증명하는지 적혀 있지 않아 **끝낼 수가 없다**. 실제 근거는 따로 있다 — Spring Boot **3.5.11 정식** 에서 19.6일 무중단(Full GC 0회, 오류 0). 검증 가능한 항목으로 다시 쓰기 전에는 닫아 둔다

**1.2 보안 수정**
- [x] `WeatherInterface`의 하드코딩 API 키 → `weather.properties` 또는 환경 변수로 이동  ← **됨**: 평문 API 키 0건 — env 참조
- [x] `application.properties`의 DB 자격증명 → 환경 변수 분리  ← **됨**: `18ee419` 로 env 화, **배포 jar 3줄 전부 env 참조 실측**(2026-09-16)
- [x] Actuator: `include=*` → `include=health,info,metrics` 제한  ← **됨**: `include=${ACTUATOR_EXPOSE:health,info}` 로 제한됨
- [x] Gemini API 키 환경 변수 통일  ← **됨**: Gemini 키 env 참조

**1.3 에러 핸들링**
- [~] 외부 API 호출 실패 시 기본값/캐시 폴백  ← **절반 됨**(2026-09-16 전수 확인): **기본값 폴백은 이미 있다.** 외부를 부르는 5개(`Air`·`Emergency`·`News`·`Traffic`·`Weather`Interface)가 **전부 `ReactiveHttp` 단일 길목**을 쓰고 **직접 호출 0건**이다. 거기에 `URI.create`(재인코딩 함정 대비) · **타임아웃 8초** · 4xx 제외 지수 재시도 · **`doOnError` 로 warn 기록** 후 `Mono.empty()` 폴백이 걸려 있다 — **조용히 삼키지 않는다**.
  ❌ **남은 것은 "캐시 폴백" 하나**: 실패 시 빈 값이라 카드가 **비어 보인다**. 마지막 성공값을 신선도 표시와 함께 보여주려면 캐시 계층이 필요한데 **지금은 없다**(`osh.briefing.cache-ttl-ms` 는 AI 브리핑 전용). ⇒ 기능 추가이므로 별도 판단.
- [x] SSE 연결 에러 처리 표준화  ← **됨**: `onError`/`completeWithError` 로 처리
- [x] 글로벌 예외 핸들러 추가  ← **됨**: `@RestControllerAdvice` 존재

### Phase 2 — UI 리뉴얼 (M3) (4주)

**2.1 레이아웃 재설계**
- [-] M3 Navigation Rail (좌측) + Top App Bar  ← **안 함**: 실사용 근거(2026-09-16, nginx 접근 로그 전수): **`/osh/` UI 접근 1회**(그마저 오늘 11:42 의 `curl` 이다) vs `/myapi/api/*` **68,433회**. **아무도 안 여는 화면의 외형**을 바꾸는 일이다
- [x] 대시보드: 그리드 카드 레이아웃 (반응형)  ← **됨**: `grid-template-columns` 반응형
- [-] 카드 표시/숨기기 + 순서 커스터마이징  ← **안 함**: 위와 같은 이유 + myapi PLAN 에 **같은 항목 3건**이 따로 있다(중복 설계)
- [x] 다크/라이트 모드 토글  ← **됨**: 다크/라이트 토글 있음

**2.2 데이터 카드 개선**
- [x] 날씨 카드: 온도, 습도, 풍속을 M3 카드에 아이콘과 함께 표시  ← **됨**: 습도 포함 표시
- [-] 교통/재난 카드: 경고 수준별 컬러 코딩 (Error 토큰)  ← **안 함**: M3 리뉴얼 묶음. 화면 자체가 안 쓰인다
- [x] 뉴스 카드: 헤드라인 리스트, 클릭 시 상세  ← **됨**: 뉴스 카드 구현
- [-] 시스템 카드: CPU/메모리 게이지 (SVG 원형 차트)  ← **안 함**: M3 리뉴얼 묶음. ⚠️단 `/myapi/api/system/status` 는 **46,734회**로 가장 많이 쓰이는 API 다 — 값은 소비되고 있고 **osh 화면만 안 쓰인다**

**2.3 반응형 대응**
- [-] 모바일: 1열 카드 스택  ← **안 함**: M3 리뉴얼 묶음
- [x] 태블릿: 2열 그리드  ← **됨**: 600/840 브레이크포인트
- [-] 데스크톱: 3~4열 그리드  ← **안 함**: M3 리뉴얼 묶음
- [-] M3 브레이크포인트: compact(<600), medium(600~840), expanded(840+)  ← **안 함**: M3 리뉴얼 묶음

### Phase 3 — 기능 확장 (4주)

**3.1 뉴스 키워드 필터**
- [x] 관심 키워드 등록 UI  ← **됨**: 키워드 UI 있음
- [ ] 키워드 매칭 뉴스만 SSE 스트림
- [x] 키워드별 뉴스 카운트 배지  ← **됨**: 카운트 배지 있음

**3.2 대시보드 뷰 프리셋**
- [-] 출근 전: 날씨 + 교통 중심  ← **안 함**: 뷰 프리셋 묶음. 실사용 근거(2026-09-16, nginx 접근 로그 전수): **`/osh/` UI 접근 1회**(그마저 오늘 11:42 의 `curl` 이다) vs `/myapi/api/*` **68,433회**. myapi 에도 **같은 4건**이 있다
- [-] 업무 중: 뉴스 + 시스템 중심  ← **안 함**: 뷰 프리셋 묶음
- [-] 퇴근 후: 생활정보 중심  ← **안 함**: 뷰 프리셋 묶음
- [-] 시간대별 자동 전환 스케줄  ← **안 함**: 뷰 프리셋 묶음

**3.3 Gemini 대시보드 어시스턴트**
- [-] 현재: 단순 요약 API  ← **항목 아님**: 이건 할 일이 아니라 *당시 현황 서술*이다. 체크박스로 적힌 것 자체가 잘못
- [~] 개선: 대시보드 데이터에 대한 대화형 질의 ("오늘 날씨 어때?", "교통 상황은?")  ← **다르게 해결**: **HARU 가 이미 그 역할을 한다** — 활성 도구 20개에 `osh_briefing` 이 있어 대화로 osh 데이터를 묻는다. 여기에 또 만들면 **같은 기능이 두 곳**이 된다
- [~] 채팅 패널 UI (M3 Bottom Sheet 또는 Side Panel)  ← **다르게 해결**: 위와 같다. 대화 진입점은 **HARU 한 곳**으로 모으는 것이 생태계 설계다

**3.4 SSE 안정성**
- [x] 클라이언트 자동 재연결 (지수 백오프)  ← **됨**: 재연결 구현
- [x] heartbeat 메시지 (30초)  ← **됨**: heartbeat 구현
- [x] 연결 상태 표시 (M3 Snackbar)  ← **됨**: 연결 상태 표시

### Phase 4 — myAPI 통합 검토 + 안정화 (3주)

**4.1 myAPI 통합 검토**
- [x] 중복 기능 정리: 날씨, 뉴스, 시스템 모니터링  ← **됨**: osh 가 뉴스 DB를 버리고 myapi 를 소비(`MyapiNewsClient`) — 중복이 실제로 제거됨
- [-] osh 고유 기능(교통/재난 SSE) → myAPI로 마이그레이션 방안  ← **안 함**: 119(역할 분리)를 택했으므로 마이그레이션은 하지 않는다. 둘은 택일이었고 답이 정해졌다
- [x] 또는 osh를 "실시간 모니터링 특화", myAPI를 "생산성 도구 특화"로 역할 분리  ← **됨**: **이 방향으로 갔다** — osh=실시간 대시보드, myapi=공용 데이터 허브로 역할이 갈렸다

**4.2 테스트**
- [x] 외부 API mock 기반 단위 테스트  ← **됨**: 테스트 11개 파일
- [x] SSE 통합 테스트  ← **됨**: SSE 테스트 존재
- [x] 장시간 운영 안정성 테스트 (메모리 누수 점검)  ← **됨**(2026-09-16): ★소크 테스트를 새로 쓰지 않고 **이미 19일 14시간 돌고 있던 라이브 프로세스**를 쟀다 — 그게 곧 장시간 시험이다.
  **누수 없음**: 강제 Full GC(511ms) 후 힙 **145MB → 54.7MB**, Old **51.5% → 34.1%**. 누수면 Full GC 후에도 안 내려간다.
  19.6일간 **Full GC 0회** · GC 총 102초(가동시간의 **0.006%**) · 힙 커밋 256MB · RSS 497MB · GC 후 오류 0.
  ⚠️ 남은 관찰거리 둘: ①**`-Xmx` 가 없다**(JVM 기본 = RAM 1/4 ≈ 2GB). 지금은 G1 이 256MB 로만 잡고 있어 문제가 안 나지만, `.25` 는 JVM 11개가 사는 공유 호스트라 **상한을 명시하는 편이 안전**하다(my-computer 는 `scripts/jvm-opts.sh` 로 유도한다). ②**Metaspace 가 커밋의 99.3%**(100MB/100.9MB) — 예약이 1152MB 라 당장 문제는 아니지만 계속 오르면 클래스 누수 신호다.

---

## 5. 기술 스택

| 구분 | 기술 | 비고 |
|------|------|------|
| 언어 | Java 17 | — |
| 프레임워크 | Spring Boot 3.4.x (정식) | 스냅샷 → 정식 |
| 웹 | Spring Web + WebFlux (SSE) | — |
| 템플릿 | Thymeleaf | 기존 유지 |
| DB | MySQL/MariaDB + H2 | — |
| 실시간 | SSE (Flux) | — |
| AI | Gemini API | — |
| 디자인 | M3 원칙 + CSS 커스텀 | 신규 적용 |
